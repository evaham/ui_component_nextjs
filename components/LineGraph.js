'use client';

export default function LineGraph() {
  return (
    <div className="h-64 p-4 rounded-xl bg-white shadow-xs transition-all">
      <div className="text-lg font-semibold mb-2">라인 그래프</div>
      <div className="h-full flex items-center justify-center text-gray-400">
        {/* 여기에 라인 그래프를 그리는 코드가 들어갑니다. */}
        라인 그래프 영역
      </div>
    </div>
  );
}